- content_for :devise_form do
  %h2.pt-4.mb-8.text-4xl.font-bold.heading Sign up
  = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
    = render "devise/shared/error_messages", resource: resource
    .input-group
      = f.label :first_name, class:"label"
      = f.text_field :first_name, class:"input", class: "input input-bordered"
      = f.label :last_name, class: "label"
      = f.text_field :last_name, class:"input", class: "input input-bordered"
    .input-group
      = f.label :email, class:"label"
      = f.email_field :email, autocomplete: "email", class:"input", class: "input input-bordered"
    .input-group
      .flex
        = f.label :password, class: "label"
        - if @minimum_password_length
          %span.pl-1.text-xs.text-grey-dark
            %em
              (#{@minimum_password_length} characters minimum)
      = f.password_field :password, autocomplete: "new-password", class: "input input-bordered"
    .input-group
      = f.label :password_confirmation, class:"label"
      = f.password_field :password_confirmation, autocomplete: "new-password", class: "input input-bordered"
    .input-group
      = f.submit "Sign up", class: "btn btn-outline my-4"
    %hr.mt-6.border/
  = render "devise/shared/links"
= render "devise/shared/form_wrap"
