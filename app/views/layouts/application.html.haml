!!!
%html.antialiased
  %body
    = render "shared/head"
    = render "shared/flash_notice"
    = render "shared/header"
    %main.px-4.lg:px-10
      = content_for?(:content) ? yield(:content) : yield