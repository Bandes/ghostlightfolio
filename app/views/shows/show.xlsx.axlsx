wb = xlsx_package.workbook

wb.add_worksheet(name: show.name) do |sheet|
  columns = show.columns_for_chart
  sheet.add_row columns
  # Create entries for each item
  show.act_role_chart_data.each do |row|
    sheet.add_row row
  end
end
